<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dexterapp.shopavenues">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopAvenues">
        <activity android:name=".activity.CelebrityDetailsActivity"></activity>
        <activity android:name=".activity.AcademyDetailsActivity" />
        <activity android:name=".activity.FilterAcademyActivity" />
<!--        <activity android:name=".activity.AcademyListActivity" />-->
        <activity android:name=".activity.CheckoutActivity" />
        <activity android:name=".activity.ProductDetailsActivity" />
        <activity android:name=".activity.FilterActivity" />
        <activity android:name=".activity.TransactionDetailsActivity" />
        <activity android:name=".activity.TransactionLIstActivity" />
        <activity android:name=".activity.EditAddressActivity" />
        <activity android:name=".activity.AddAddressActivity" />
        <activity android:name=".activity.AddressListActivity" />
        <activity android:name=".activity.ProfileActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>